DATABASE_URL="file:./dev.db"

# 32+ char random string. Generate one with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
IRON_SESSION_PASSWORD="change-me-to-a-random-32-char-secret-please-do-not-ship-this-to-prod"

# URL of the Python FastAPI service that serves /api/ai/*
PY_AI_URL="http://127.0.0.1:5000"
