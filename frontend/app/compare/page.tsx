"use client";

import { useEffect } from "react";

const pageCss = "\n    :root{\n      --bg:#f5f2ea;\n      --ink:#11131a;\n      --muted:#697083;\n      --navy:#020816;\n      --navy-2:#08142f;\n      --navy-3:#263145;\n      --gold:#f2c94c;\n      --gold-deep:#ddb22a;\n      --white:#ffffff;\n      --line:rgba(17,19,26,.08);\n      --grid:rgba(255,255,255,.13);\n      --shadow:0 18px 40px rgba(0,0,0,.14);\n      --soft-shadow:0 18px 30px rgba(25,30,42,.08);\n      --ease-smooth:cubic-bezier(.22,1,.36,1);\n      --success:#3ecf8e;\n      --warning:#ffb020;\n      --danger:#ff6b6b;\n      --soft:#f3f5f8;\n      --card:#ffffff;\n    }\n    *{box-sizing:border-box}\n    html{scroll-behavior:smooth}\n    body{margin:0;font-family:Inter,Arial,Helvetica,sans-serif;background:var(--bg);color:var(--ink);overflow-x:hidden}\n    ::selection{background:rgba(242,201,76,.28);color:#fff}\n    h1,h2,h3,h4{font-family:Georgia,\"Times New Roman\",serif;margin:0}\n    p{margin:0}\n    a{text-decoration:none;color:inherit}\n    button{font:inherit}\n    .container{width:min(1180px,calc(100% - 56px));margin:0 auto}\n\n    .hero-shell{position:relative;overflow:hidden;color:#fff;background:linear-gradient(110deg,#020816 0%, #08142f 38%, #263145 72%, #d7d5d1 125%),radial-gradient(circle at 18% 28%, rgba(8,76,198,.18), transparent 36%)}\n    .hero-shell::before{content:\"\";position:absolute;inset:0;background-image:linear-gradient(var(--grid) 1px, transparent 1px),linear-gradient(90deg,var(--grid) 1px, transparent 1px);background-size:92px 92px;pointer-events:none}\n    .hero-shell::after{content:\"\";position:absolute;width:520px;height:520px;right:-80px;top:110px;border-radius:50%;background:radial-gradient(circle, rgba(242,201,76,.20) 0%, rgba(242,201,76,.10) 28%, rgba(242,201,76,0) 68%);filter:blur(18px);pointer-events:none;animation:floatGlow 8s ease-in-out infinite}\n    @keyframes floatGlow{0%,100%{transform:translateY(0) translateX(0) scale(1)}50%{transform:translateY(-16px) translateX(-8px) scale(1.03)}}\n\n    .nav{position:sticky;top:0;z-index:50;width:100%;padding:22px 0;backdrop-filter:blur(14px);background:linear-gradient(180deg, rgba(2,8,22,.72), rgba(2,8,22,.32));border-bottom:1px solid rgba(255,255,255,.06)}\n    .nav-inner{display:flex;align-items:center;justify-content:space-between;gap:16px}\n    .brand{display:flex;align-items:center;gap:12px;font-weight:800;letter-spacing:.02em;white-space:nowrap}\n    .brand-mark{width:42px;height:42px;border-radius:12px;display:grid;place-items:center;background:linear-gradient(135deg, rgba(214,178,93,0.22), rgba(255,255,255,0.08));border:1px solid rgba(214,178,93,0.24);box-shadow:inset 0 1px 0 rgba(255,255,255,0.08);color:#f3dfaa;font-size:18px;overflow:hidden;flex:none}\n    .brand-mark img{width:100%;height:100%;object-fit:contain;display:block}\n    .nav-links{display:flex;align-items:center;gap:14px;flex-wrap:wrap}\n    .nav-link,.nav-ghost{padding:10px 14px;border-radius:999px;color:rgba(233,238,247,0.72);transition:all 260ms cubic-bezier(.2,.8,.2,1)}\n    .nav-link:hover,.nav-ghost:hover,.nav-link.active{color:#fff;background:rgba(255,255,255,0.05)}\n    .nav-cta{padding:11px 18px;border-radius:999px;color:#111;font-weight:700;background:linear-gradient(135deg,#d6b25d,#f3dfaa);box-shadow:0 10px 30px rgba(214,178,93,0.22);transition:transform 260ms cubic-bezier(.2,.8,.2,1), box-shadow 260ms cubic-bezier(.2,.8,.2,1)}\n    .nav-cta:hover{transform:translateY(-2px);box-shadow:0 16px 34px rgba(214,178,93,0.28)}\n\n    .profile-wrapper{position:relative}\n    .profile-trigger{width:42px;height:42px;border:none;border-radius:50%;display:grid;place-items:center;font-weight:800;cursor:pointer;background:linear-gradient(135deg,#d6b25d,#f3dfaa);color:#111;box-shadow:0 10px 24px rgba(214,178,93,0.24)}\n    .profile-card{position:absolute;top:56px;right:0;width:300px;background:rgba(9,18,35,0.96);border:1px solid rgba(255,255,255,0.08);border-radius:22px;padding:18px;display:none;flex-direction:column;gap:16px;box-shadow:0 24px 54px rgba(0,0,0,.35);backdrop-filter:blur(18px);z-index:60}\n    .profile-card.active{display:flex}\n    .profile-card-top{display:flex;gap:12px;align-items:flex-start}\n    .profile-avatar-large{width:52px;height:52px;border-radius:50%;display:grid;place-items:center;font-weight:800;background:linear-gradient(135deg,#d6b25d,#f3dfaa);color:#111;flex:0 0 52px}\n    .profile-user-info strong{display:block;font-size:15px;color:#fff;margin-bottom:4px}\n    .profile-user-info span{display:block;font-size:13px;color:rgba(255,255,255,0.62)}\n    .profile-role{margin-top:10px;display:inline-flex;align-items:center;padding:6px 10px;border-radius:999px;font-size:11px;font-weight:800;letter-spacing:.05em;text-transform:uppercase;width:fit-content}\n    .profile-role.buyer{background:rgba(71,209,140,0.14);color:#47d18c;border:1px solid rgba(71,209,140,0.24)}\n    .profile-role.seller{background:rgba(255,107,107,0.14);color:#ff8a8a;border:1px solid rgba(255,107,107,0.24)}\n    .profile-role.investor{background:rgba(214,178,93,0.16);color:#f3dfaa;border:1px solid rgba(214,178,93,0.26)}\n    .profile-card-links{display:flex;flex-direction:column;gap:8px}\n    .profile-card-links a{padding:11px 12px;border-radius:12px;font-size:14px;color:rgba(255,255,255,0.74);transition:background .25s ease, color .25s ease}\n    .profile-card-links a:hover{background:rgba(255,255,255,0.06);color:#fff}\n    .auth-nav{display:flex;align-items:center;gap:14px;flex-wrap:wrap}\n\n    .hero{position:relative;z-index:1;padding:48px 0 94px}\n    .hero-grid{display:grid;grid-template-columns:1.02fr .98fr;gap:24px;align-items:end}\n    .eyebrow{display:inline-flex;align-items:center;gap:10px;padding:10px 16px;border:1px solid rgba(255,255,255,.14);border-radius:999px;background:rgba(255,255,255,.06);color:#f3dfaa;font-size:.86rem;font-weight:700;letter-spacing:.04em;text-transform:uppercase;backdrop-filter:blur(10px);margin-bottom:18px}\n    .hero h1{font-size:clamp(2.8rem,5vw,4.8rem);line-height:.96;letter-spacing:-.03em;max-width:10ch;margin-bottom:16px}\n    .hero p{max-width:720px;color:rgba(255,255,255,.82);font-size:1rem;line-height:1.9}\n    .hero-stats{display:grid;grid-template-columns:repeat(3,1fr);gap:12px}\n    .hero-stat{padding:16px;border-radius:18px;background:rgba(255,255,255,.08);border:1px solid rgba(255,255,255,.08)}\n    .hero-stat strong{display:block;font-size:1.5rem;margin-bottom:6px}\n    .hero-stat span{color:rgba(255,255,255,.74);font-size:.86rem;line-height:1.5}\n\n    main{padding:0 0 86px}\n    .compare-shell{margin-top:-52px;position:relative;z-index:4;display:grid;gap:24px}\n    .panel{background:#fff;border-radius:30px;border:1px solid rgba(17,19,26,.06);box-shadow:var(--soft-shadow)}\n    .summary-panel{padding:22px}\n    .summary-top{display:flex;justify-content:space-between;align-items:flex-start;gap:16px;flex-wrap:wrap}\n    .summary-top h2{font-size:2rem;margin-bottom:6px}\n    .muted{color:var(--muted);line-height:1.7}\n    .summary-actions{display:flex;gap:10px;flex-wrap:wrap}\n    .btn{min-height:46px;padding:0 16px;border:none;border-radius:14px;font-weight:800;cursor:pointer;transition:all .22s ease;display:inline-flex;align-items:center;justify-content:center;text-align:center}\n    .btn:hover{transform:translateY(-2px);box-shadow:0 10px 24px rgba(0,0,0,.16)}\n    .btn.primary{background:#0f1728;color:#fff}\n    .btn.secondary{background:#eef3fb;color:#243244;border:1px solid rgba(36,50,68,.12)}\n    .btn.gold{background:#fff7db;color:#6a4b00;border:1px solid rgba(221,178,42,.28)}\n    .summary-row{display:grid;grid-template-columns:repeat(4,1fr);gap:12px;margin-top:18px}\n    .metric-card{padding:16px;border-radius:20px;background:var(--soft);border:1px solid rgba(17,19,26,.05)}\n    .metric-card strong{display:block;font-size:1.3rem;margin-bottom:6px}\n    .metric-card span{color:var(--muted);font-size:.9rem;line-height:1.5}\n\n    .compare-panel{overflow:hidden}\n    .compare-scroll{overflow-x:auto;padding:0 22px 22px}\n    .compare-table{min-width:980px;border-collapse:separate;border-spacing:0;width:100%}\n    .compare-table th,.compare-table td{padding:16px;border-bottom:1px solid rgba(17,19,26,.08);vertical-align:top;background:#fff}\n    .compare-table thead th{position:sticky;top:0;z-index:2;background:#fff}\n    .compare-table th.label-col,.compare-table td.label-col{position:sticky;left:0;z-index:3;background:#fbfbfc;min-width:190px;max-width:190px;border-right:1px solid rgba(17,19,26,.06)}\n    .compare-table thead th.label-col{z-index:5}\n    .property-head{min-width:260px}\n    .property-media{position:relative;height:180px;border-radius:20px;overflow:hidden;background:#e9edf4;margin-bottom:14px}\n    .property-media img{width:100%;height:100%;object-fit:cover;display:block}\n    .trust-pill,.verified-pill,.mini-pill{display:inline-flex;align-items:center;gap:8px;border-radius:999px;padding:8px 12px;font-size:.78rem;font-weight:800}\n    .trust-pill.ai{background:rgba(62,207,142,.14);color:#0c9152;border:1px solid rgba(62,207,142,.22)}\n    .trust-pill.manual{background:rgba(255,176,32,.16);color:#986300;border:1px solid rgba(255,176,32,.24)}\n    .verified-pill{background:rgba(71,209,140,.14);color:#0c9152;border:1px solid rgba(71,209,140,.22)}\n    .mini-pill{background:#f4f6f9;color:#334155;border:1px solid rgba(51,65,85,.08)}\n    .property-title{font-size:1.35rem;margin-bottom:8px}\n    .property-location{color:var(--muted);margin-bottom:12px;line-height:1.6}\n    .price{font-size:1.55rem;font-weight:800;color:#0f1728;margin-bottom:12px}\n    .head-pills,.extras-wrap{display:flex;gap:8px;flex-wrap:wrap}\n    .cell-title{font-weight:800;color:#0f1728;margin-bottom:6px}\n    .cell-text{color:var(--muted);line-height:1.7}\n    .winner{display:inline-flex;align-items:center;gap:8px;padding:8px 10px;border-radius:999px;background:#0f1728;color:#fff;font-size:.76rem;font-weight:800}\n    .stack{display:grid;gap:8px}\n    .empty-state{padding:38px 26px;text-align:center}\n    .empty-state h2{font-size:2rem;margin-bottom:12px}\n    .empty-state p{max-width:680px;margin:0 auto;color:var(--muted);line-height:1.8}\n    .empty-actions{display:flex;justify-content:center;gap:12px;flex-wrap:wrap;margin-top:22px}\n\n    @media (max-width: 980px){\n      .nav-inner,.hero-grid,.summary-row{grid-template-columns:1fr;display:grid}\n      .nav-links,.auth-nav{width:100%;justify-content:center}\n      .hero-stats{grid-template-columns:1fr}\n      .summary-row{grid-template-columns:1fr 1fr}\n    }\n    @media (max-width: 640px){\n      .container{width:min(1180px,calc(100% - 28px))}\n      .summary-row{grid-template-columns:1fr}\n      .compare-scroll{padding:0 14px 14px}\n      .summary-panel{padding:18px}\n      .hero{padding:38px 0 84px}\n    }\n  ";
const pageHtml = "\n<header class=\"hero-shell\">\n<div class=\"nav\">\n<div class=\"container nav-inner\">\n<a class=\"brand\" href=\"/\">\n<span class=\"brand-mark\"><img alt=\"SmartEstate logo\" src=\"/favicon.ico\"/></span>\n<span>SmartEstate</span>\n</a>\n<div class=\"nav-links\">\n<a class=\"nav-link\" href=\"/\">Home</a>\n<a class=\"nav-link\" href=\"/marketplace\">Marketplace</a>\n<a class=\"nav-link\" href=\"/property-estimator\">Property AI</a>\n<a class=\"nav-link\" href=\"/areas\">Areas</a>\n<a class=\"nav-link\" href=\"/loans\">Loans</a>\n<a class=\"nav-link\" href=\"/land\">Land</a>\n<a class=\"nav-link\" href=\"/dashboard\">Dashboard</a>\n<a class=\"nav-link active\" href=\"/compare\">Compare</a>\n<a class=\"nav-cta\" href=\"/post-property\">Post Property</a>\n</div>\n<div class=\"auth-nav\" id=\"guestActions\">\n<a class=\"nav-link\" href=\"/login\">Login</a>\n<a class=\"nav-cta\" href=\"/signup\">Create account</a>\n</div>\n<div class=\"profile-wrapper\" id=\"profileWrapper\" style=\"display:none;\">\n<button class=\"profile-trigger\" id=\"profileTrigger\" type=\"button\">SE</button>\n<div class=\"profile-card\" id=\"profileCard\">\n<div class=\"profile-card-top\">\n<div class=\"profile-avatar-large\" id=\"profileAvatarLarge\">SE</div>\n<div class=\"profile-user-info\">\n<strong id=\"profileName\">SmartEstate User</strong>\n<span id=\"profileEmail\">Sign in to continue</span>\n<div class=\"profile-role buyer\" id=\"profileRole\">Buyer</div>\n</div>\n</div>\n<div class=\"profile-card-links\">\n<a href=\"/dashboard\">Dashboard</a>\n<a href=\"/marketplace\">Marketplace</a>\n<a href=\"/post-property\">Post property</a>\n<a href=\"#\" id=\"logoutBtn\">Logout</a>\n</div>\n</div>\n</div>\n</div>\n</div>\n<div class=\"container hero\">\n<div class=\"hero-grid\">\n<div>\n<span class=\"eyebrow\">SmartEstate decision studio</span>\n<h1>Compare your shortlisted listings</h1>\n<p>Review up to three properties side by side with price, size, yield, demand, AI pricing status, extras, seller trust, and quick actions in one premium board.</p>\n</div>\n<div class=\"hero-stats\">\n<div class=\"hero-stat\"><strong id=\"statCompareCount\">0</strong><span>Listings in compare</span></div>\n<div class=\"hero-stat\"><strong id=\"statBestYield\">\u2014</strong><span>Best yield</span></div>\n<div class=\"hero-stat\"><strong id=\"statBestDemand\">\u2014</strong><span>Best demand</span></div>\n</div>\n</div>\n</div>\n</header>\n<main>\n<div class=\"container compare-shell\">\n<section class=\"panel summary-panel\" id=\"summaryPanel\" style=\"display:none;\">\n<div class=\"summary-top\">\n<div>\n<h2>Comparison summary</h2>\n<p class=\"muted\">Use this table to spot the strongest price, investor signal, and fit before opening the full listing page.</p>\n</div>\n<div class=\"summary-actions\">\n<a class=\"btn secondary\" href=\"/dashboard\">Back to dashboard</a>\n<a class=\"btn gold\" href=\"/marketplace\">Add more from marketplace</a>\n<button class=\"btn primary\" id=\"clearCompareBtn\" type=\"button\">Clear compare board</button>\n</div>\n</div>\n<div class=\"summary-row\" id=\"summaryRow\"></div>\n</section>\n<section class=\"panel compare-panel\" id=\"comparePanel\" style=\"display:none;\">\n<div class=\"compare-scroll\">\n<table class=\"compare-table\">\n<thead id=\"compareHead\"></thead>\n<tbody id=\"compareBody\"></tbody>\n</table>\n</div>\n</section>\n<section class=\"panel empty-state\" id=\"emptyState\">\n<h2>Your compare board is empty</h2>\n<p>Add listings from the marketplace, property details page, or your dashboard compare tab. SmartEstate already supports up to three picks, so this page turns those picks into a real side-by-side decision view.</p>\n<div class=\"empty-actions\">\n<a class=\"btn primary\" href=\"/marketplace\">Browse marketplace</a>\n<a class=\"btn secondary\" href=\"/dashboard\">Open dashboard</a>\n</div>\n</section>\n</div>\n</main>\n\n";
const pageScript = "\n    const API_BASE = 'http://127.0.0.1:5000';\n    const USER_KEY = 'smartestateUser';\n    const DASHBOARD_CACHE_PREFIX = 'smartestateDashboardCacheV2:';\n    const LEGACY_LISTINGS_KEY = 'smartestateListings';\n    const LEGACY_SELECTED_KEY = 'smartestateSelectedListing';\n    const LEGACY_COMPARE_KEY = 'smartestateCompare';\n    const COMPARE_SNAPSHOT_KEY = 'smartestateCompareSnapshot';\n    const MARKETPLACE_CACHE_KEY = 'smartestateMarketplaceCacheV2';\n\n    const currentUser = (() => {\n      try { return JSON.parse(localStorage.getItem(USER_KEY) || 'null'); }\n      catch (e) { return null; }\n    })();\n\n    const guestActions = document.getElementById('guestActions');\n    const profileWrapper = document.getElementById('profileWrapper');\n    const profileTrigger = document.getElementById('profileTrigger');\n    const profileCard = document.getElementById('profileCard');\n    const profileAvatarLarge = document.getElementById('profileAvatarLarge');\n    const profileName = document.getElementById('profileName');\n    const profileEmail = document.getElementById('profileEmail');\n    const profileRole = document.getElementById('profileRole');\n\n    const state = { compare: [], compareIds: [] };\n\n    function safeJsonParse(raw, fallback){ try { return JSON.parse(raw); } catch (error) { return fallback; } }\n    function initialsFromName(name){ return String(name || 'SE').split(/\\s+/).filter(Boolean).slice(0,2).map(part => part[0]).join('').toUpperCase() || 'SE'; }\n    function roleLabel(role){ const value = String(role || 'buyer').toLowerCase(); return value.charAt(0).toUpperCase() + value.slice(1); }\n    function formatPrice(value){ return new Intl.NumberFormat('en-US').format(Number(value || 0)); }\n    function fallbackImage(title){ return `https://placehold.co/1200x800/f3f5f8/11131a?text=${encodeURIComponent(title || 'SmartEstate')}`; }\n    function imageSrc(raw){ if(!raw) return fallbackImage('SmartEstate'); return raw.startsWith('http') || raw.startsWith('data:') ? raw : `${API_BASE}${raw.startsWith('/') ? '' : '/'}${raw}`; }\n\n    function normalizeImages(rawImages, title='Listing'){\n      if(Array.isArray(rawImages) && rawImages.length){\n        const normalized = rawImages.map((item, index) => {\n          const src = typeof item === 'string' ? item : item?.src || item?.image || item?.url;\n          if(!src) return null;\n          return { src: imageSrc(src), label: item?.label || (index === 0 ? 'Main View' : `Gallery ${index}`) };\n        }).filter(Boolean);\n        if(normalized.length) return normalized;\n      }\n      return [{ src: fallbackImage(title), label: title }];\n    }\n\n    function normalizeListing(item){\n      if(!item || item.id == null) return null;\n      const estimatedYield = item.estimatedYield ?? item.estimated_yield ?? null;\n      const demandScore = item.demandScore ?? item.demand_score ?? null;\n      const aiConfidence = item.aiConfidence ?? item.ai_confidence ?? null;\n      const trust = item.trust || (String(item.trustLabel || item.trust_label || '').toLowerCase().includes('ai') ? 'ai' : 'manual');\n      return {\n        ...item,\n        id: String(item.id),\n        title: item.title || 'SmartEstate Listing',\n        type: item.type || item.propertyType || item.property_type || 'Property',\n        price: Number(item.price || 0),\n        bedrooms: Number(item.bedrooms || 0),\n        bathrooms: Number(item.bathrooms || 0),\n        size: Number(item.size || item.area || 0),\n        estimatedYield: estimatedYield == null ? null : Number(estimatedYield),\n        demandScore: demandScore == null ? null : Number(demandScore),\n        aiConfidence: aiConfidence == null ? null : Number(aiConfidence),\n        trust,\n        verified: Boolean(item.verified),\n        status: String(item.status || 'available').toLowerCase(),\n        extras: Array.isArray(item.extras) ? item.extras : [],\n        images: normalizeImages(item.images || item.gallery || [], item.title),\n        trustLabel: item.trustLabel || item.trust_label || (trust === 'ai' ? 'AI Price Assisted' : 'Seller Price Only'),\n        seller: item.seller || item.sellerName || 'SmartEstate Seller',\n        sellerNote: item.sellerNote || item.seller_note || 'Ready for buyer inquiries',\n        location: item.location || 'Amman',\n        areaName: item.areaName || item.area_name || item.type || 'Property area',\n        description: item.description || 'No description added yet.'\n      };\n    }\n\n    function renderNav(){\n      if(currentUser && profileWrapper && guestActions){\n        guestActions.style.display = 'none';\n        profileWrapper.style.display = 'block';\n        const initials = initialsFromName(currentUser.fullName || currentUser.name || 'SE');\n        profileTrigger.textContent = initials;\n        profileAvatarLarge.textContent = initials;\n        profileName.textContent = currentUser.fullName || currentUser.name || 'SmartEstate User';\n        profileEmail.textContent = currentUser.email || 'No email';\n        profileRole.textContent = roleLabel(currentUser.role);\n        profileRole.className = `profile-role ${String(currentUser.role || 'buyer').toLowerCase()}`;\n      }\n    }\n\n    function bindNavEvents(){\n      profileTrigger?.addEventListener('click', event => { event.stopPropagation(); profileCard.classList.toggle('active'); });\n      document.addEventListener('click', event => { if(profileWrapper && !profileWrapper.contains(event.target)) profileCard?.classList.remove('active'); });\n      document.getElementById('logoutBtn')?.addEventListener('click', event => {\n        event.preventDefault();\n        localStorage.removeItem(USER_KEY);\n        if(currentUser?.id) sessionStorage.removeItem(`${DASHBOARD_CACHE_PREFIX}${currentUser.id}`);\n        location.href = '/';\n      });\n    }\n\n    function getLegacyListingPool(){\n      const selected = safeJsonParse(localStorage.getItem(LEGACY_SELECTED_KEY) || 'null', null);\n      const stored = safeJsonParse(localStorage.getItem(LEGACY_LISTINGS_KEY) || '[]', []);\n      const marketplaceCache = safeJsonParse(sessionStorage.getItem(MARKETPLACE_CACHE_KEY) || 'null', null);\n      const cachedListings = Array.isArray(marketplaceCache?.listings) ? marketplaceCache.listings : [];\n      const pool = [...stored, ...cachedListings];\n      if(selected) pool.unshift(selected);\n      const map = new Map();\n      for(const item of pool){ if(!item || item.id == null) continue; map.set(String(item.id), item); }\n      return Array.from(map.values());\n    }\n\n    function hydrateCompareFromIds(ids){\n      const pool = getLegacyListingPool();\n      return ids.map(id => pool.find(item => String(item.id) === String(id))).filter(Boolean).map(normalizeListing).filter(Boolean);\n    }\n\n    function extractItems(raw){\n      if(!raw) return [];\n      if(Array.isArray(raw)){\n        if(raw.length && typeof raw[0] === 'object') return raw.map(normalizeListing).filter(Boolean);\n        if(raw.length) return hydrateCompareFromIds(raw);\n      }\n      if(Array.isArray(raw.compare)) return raw.compare.map(normalizeListing).filter(Boolean);\n      return [];\n    }\n\n    function getAllCachedCompareSources(){\n      const results = [];\n      results.push(...extractItems(safeJsonParse(localStorage.getItem(COMPARE_SNAPSHOT_KEY) || '[]', [])));\n      results.push(...extractItems(safeJsonParse(localStorage.getItem(LEGACY_COMPARE_KEY) || '[]', [])));\n      for(let i = 0; i < sessionStorage.length; i += 1){\n        const key = sessionStorage.key(i);\n        if(!key || !key.startsWith(DASHBOARD_CACHE_PREFIX)) continue;\n        const parsed = safeJsonParse(sessionStorage.getItem(key) || 'null', null);\n        results.push(...extractItems(parsed));\n      }\n      return dedupeListings(results).slice(0,3);\n    }\n\n    function dedupeListings(items){\n      const map = new Map();\n      (items || []).forEach(item => { const normalized = normalizeListing(item); if(normalized) map.set(String(normalized.id), normalized); });\n      return Array.from(map.values());\n    }\n\n    function bestBy(items, key, mode='max'){\n      const valid = items.filter(item => item[key] != null && !Number.isNaN(Number(item[key])));\n      if(!valid.length) return null;\n      return valid.reduce((best, current) => {\n        if(!best) return current;\n        return mode === 'min' ? (Number(current[key]) < Number(best[key]) ? current : best) : (Number(current[key]) > Number(best[key]) ? current : best);\n      }, null);\n    }\n\n    function setStats(){\n      const items = state.compare;\n      const bestYield = bestBy(items, 'estimatedYield', 'max');\n      const bestDemand = bestBy(items, 'demandScore', 'max');\n      document.getElementById('statCompareCount').textContent = String(items.length);\n      document.getElementById('statBestYield').textContent = bestYield ? `${bestYield.estimatedYield}%` : '\u2014';\n      document.getElementById('statBestDemand').textContent = bestDemand ? `${bestDemand.demandScore}` : '\u2014';\n    }\n\n    function renderSummary(){\n      const items = state.compare;\n      const lowestPrice = bestBy(items, 'price', 'min');\n      const largestSize = bestBy(items, 'size', 'max');\n      const bestYield = bestBy(items, 'estimatedYield', 'max');\n      const bestDemand = bestBy(items, 'demandScore', 'max');\n      document.getElementById('summaryRow').innerHTML = `\n        <div class=\"metric-card\"><strong>${lowestPrice ? lowestPrice.title : '\u2014'}</strong><span>${lowestPrice ? `Lowest price \u2022 JOD ${formatPrice(lowestPrice.price)}` : 'No pricing signal yet'}</span></div>\n        <div class=\"metric-card\"><strong>${largestSize ? largestSize.title : '\u2014'}</strong><span>${largestSize ? `Largest size \u2022 ${largestSize.size} m\u00b2` : 'No size signal yet'}</span></div>\n        <div class=\"metric-card\"><strong>${bestYield ? bestYield.title : '\u2014'}</strong><span>${bestYield ? `Best yield \u2022 ${bestYield.estimatedYield}%` : 'No yield signal yet'}</span></div>\n        <div class=\"metric-card\"><strong>${bestDemand ? bestDemand.title : '\u2014'}</strong><span>${bestDemand ? `Best demand \u2022 ${bestDemand.demandScore}` : 'No demand signal yet'}</span></div>\n      `;\n    }\n\n    function cellWinner(item, reference, key){ if(!reference || item[key] == null) return ''; return String(reference.id) === String(item.id) ? '<div class=\"winner\">Best here</div>' : ''; }\n\n    function renderCompareTable(){\n      const items = state.compare;\n      const lowestPrice = bestBy(items, 'price', 'min');\n      const largestSize = bestBy(items, 'size', 'max');\n      const bestYield = bestBy(items, 'estimatedYield', 'max');\n      const bestDemand = bestBy(items, 'demandScore', 'max');\n      const bestConfidence = bestBy(items, 'aiConfidence', 'max');\n      document.getElementById('compareHead').innerHTML = `<tr><th class=\"label-col\">Property</th>${items.map(item => `<th class=\"property-head\"><div class=\"property-media\"><img src=\"${item.images[0]?.src || fallbackImage(item.title)}\" alt=\"${item.title}\"></div><h3 class=\"property-title\">${item.title}</h3><p class=\"property-location\">${item.location}</p><div class=\"price\">JOD ${formatPrice(item.price)}</div><div class=\"head-pills\"><span class=\"trust-pill ${item.trust === 'ai' ? 'ai' : 'manual'}\">${item.trustLabel}</span>${item.verified ? '<span class=\"verified-pill\">Verified seller</span>' : ''}</div><div class=\"head-pills\" style=\"margin-top:10px;\"><button class=\"btn secondary\" type=\"button\" data-open=\"${item.id}\">Open</button><button class=\"btn gold\" type=\"button\" data-remove-compare=\"${item.id}\">Remove</button></div></th>`).join('')}</tr>`;\n      const rows = [\n        { label: 'Price', render: item => `<div class=\"stack\"><div class=\"cell-title\">JOD ${formatPrice(item.price)}</div>${cellWinner(item, lowestPrice, 'price')}<div class=\"cell-text\">Listed price shown on SmartEstate.</div></div>` },\n        { label: 'Layout', render: item => `<div class=\"stack\"><div class=\"cell-title\">${item.bedrooms} bed \u2022 ${item.bathrooms} bath</div><div class=\"cell-text\">${item.size} m\u00b2 interior size.</div></div>` },\n        { label: 'Size', render: item => `<div class=\"stack\"><div class=\"cell-title\">${item.size} m\u00b2</div>${cellWinner(item, largestSize, 'size')}<div class=\"cell-text\">Useful for value-per-meter comparison.</div></div>` },\n        { label: 'Area', render: item => `<div class=\"stack\"><div class=\"cell-title\">${item.areaName}</div><div class=\"cell-text\">${item.location}</div></div>` },\n        { label: 'Investor signals', render: item => `<div class=\"stack\"><div class=\"head-pills\">${item.estimatedYield != null ? `<span class=\"mini-pill\">Yield ${item.estimatedYield}%</span>` : '<span class=\"mini-pill\">No yield</span>'}${item.demandScore != null ? `<span class=\"mini-pill\">Demand ${item.demandScore}</span>` : '<span class=\"mini-pill\">No demand</span>'}${item.aiConfidence != null ? `<span class=\"mini-pill\">AI ${item.aiConfidence}%</span>` : '<span class=\"mini-pill\">No AI confidence</span>'}</div>${cellWinner(item, bestYield, 'estimatedYield')}${cellWinner(item, bestDemand, 'demandScore')}${cellWinner(item, bestConfidence, 'aiConfidence')}</div>` },\n        { label: 'Seller trust', render: item => `<div class=\"stack\"><div class=\"cell-title\">${item.verified ? 'Verified seller' : 'Standard listing'}</div><div class=\"cell-text\">${item.seller} \u2022 ${item.sellerNote}</div></div>` },\n        { label: 'Extras', render: item => `<div class=\"extras-wrap\">${(item.extras || []).length ? item.extras.map(extra => `<span class=\"mini-pill\">${extra}</span>`).join('') : '<span class=\"mini-pill\">No extras listed</span>'}</div>` },\n        { label: 'Status', render: item => `<div class=\"stack\"><div class=\"cell-title\">${item.status === 'sold' ? 'Sold' : 'Available'}</div><div class=\"cell-text\">${item.type || 'Property'} \u2022 ${item.trust === 'ai' ? 'AI assisted pricing' : 'Seller pricing only'}</div></div>` },\n        { label: 'Description', render: item => `<div class=\"cell-text\">${item.description || 'No description added yet.'}</div>` }\n      ];\n      document.getElementById('compareBody').innerHTML = rows.map(row => `<tr><td class=\"label-col\"><div class=\"cell-title\">${row.label}</div></td>${items.map(item => `<td>${row.render(item)}</td>`).join('')}</tr>`).join('');\n    }\n\n    function renderState(){\n      const hasItems = state.compare.length > 0;\n      document.getElementById('emptyState').style.display = hasItems ? 'none' : 'block';\n      document.getElementById('summaryPanel').style.display = hasItems ? 'block' : 'none';\n      document.getElementById('comparePanel').style.display = hasItems ? 'block' : 'none';\n      setStats();\n      if(!hasItems) return;\n      renderSummary();\n      renderCompareTable();\n    }\n\n    function setCompare(items){\n      const normalized = dedupeListings(items).slice(0,3);\n      state.compare = normalized;\n      state.compareIds = normalized.map(item => String(item.id));\n      localStorage.setItem(COMPARE_SNAPSHOT_KEY, JSON.stringify(normalized));\n      localStorage.setItem(LEGACY_COMPARE_KEY, JSON.stringify(state.compareIds));\n      renderState();\n      return normalized.length > 0;\n    }\n\n    async function fetchDashboard(){\n      const cachedCompare = getAllCachedCompareSources();\n      if(cachedCompare.length) setCompare(cachedCompare);\n      if(!currentUser?.id){ renderState(); return; }\n      try {\n        const response = await fetch(`${API_BASE}/api/users/${currentUser.id}/dashboard`);\n        const data = await response.json();\n        if(!response.ok || !data.ok) throw new Error(data.message || 'Could not load compare board');\n        sessionStorage.setItem(`${DASHBOARD_CACHE_PREFIX}${currentUser.id}`, JSON.stringify(data));\n        const backendCompare = extractItems(data.compare || []);\n        if(backendCompare.length) setCompare(backendCompare);\n        else if(!state.compare.length) setCompare(cachedCompare);\n        else renderState();\n      } catch (error) {\n        console.warn(error);\n        renderState();\n      }\n    }\n\n    async function apiAction(url, options){ const response = await fetch(url, options); const data = await response.json(); if(!response.ok || !data.ok) throw new Error(data.message || 'Request failed'); return data; }\n    async function removeCompare(listingId){ const next = state.compare.filter(item => String(item.id) !== String(listingId)); setCompare(next); if(!currentUser?.id) return; await apiAction(`${API_BASE}/api/users/${currentUser.id}/compare/${listingId}`, { method:'DELETE' }); sessionStorage.removeItem(`${DASHBOARD_CACHE_PREFIX}${currentUser.id}`); await fetchDashboard(); }\n    async function clearCompare(){ const ids = state.compare.map(item => item.id); setCompare([]); if(!currentUser?.id || !ids.length) return; for(const id of ids){ await apiAction(`${API_BASE}/api/users/${currentUser.id}/compare/${id}`, { method:'DELETE' }); } sessionStorage.removeItem(`${DASHBOARD_CACHE_PREFIX}${currentUser.id}`); await fetchDashboard(); }\n\n    document.addEventListener('click', async event => {\n      const openId = event.target.getAttribute('data-open');\n      const removeId = event.target.getAttribute('data-remove-compare');\n      if(openId){ location.href = `property-details.html?id=${encodeURIComponent(openId)}`; return; }\n      if(removeId){ try { await removeCompare(removeId); } catch (error) { alert(error.message || 'Could not remove listing from compare.'); } }\n    });\n\n    document.getElementById('clearCompareBtn').addEventListener('click', async () => {\n      if(!state.compare.length) return;\n      if(!confirm('Clear all compare listings?')) return;\n      try { await clearCompare(); } catch (error) { alert(error.message || 'Could not clear compare board.'); }\n    });\n\n    renderNav();\n    bindNavEvents();\n    renderState();\n    fetchDashboard();\n  ";

export default function ComparePage() {
  useEffect(() => {
    if (!pageScript.trim()) return;
    const script = document.createElement("script");
    script.type = "text/javascript";
    script.text = pageScript;
    document.body.appendChild(script);
    document.dispatchEvent(new Event("DOMContentLoaded", { bubbles: true, cancelable: true }));
    return () => { script.remove(); };
  }, []);

  return (
    <>
      <style dangerouslySetInnerHTML={{ __html: pageCss }} />
      <div dangerouslySetInnerHTML={{ __html: pageHtml }} />
    </>
  );
}
