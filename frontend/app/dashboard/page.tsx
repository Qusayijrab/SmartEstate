"use client";

import { useEffect } from "react";

const pageCss = "\n    :root{--bg:#f5f2ea;--ink:#11131a;--muted:#697083;--navy:#020816;--navy-2:#08142f;--navy-3:#263145;--gold:#f2c94c;--gold-deep:#ddb22a;--white:#ffffff;--line:rgba(17,19,26,.08);--grid:rgba(255,255,255,.13);--shadow:0 18px 40px rgba(0,0,0,.14);--soft-shadow:0 18px 30px rgba(25,30,42,.08);--ease-smooth:cubic-bezier(.22,1,.36,1);--success:#3ecf8e;--warning:#ffb020;--danger:#ff6b6b;--soft:#f3f5f8;}\n    *{box-sizing:border-box} html{scroll-behavior:smooth} body{margin:0;font-family:Inter,Arial,Helvetica,sans-serif;background:var(--bg);color:var(--ink);animation:fadeIn .4s ease}@keyframes fadeIn{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:translateY(0)}} a{text-decoration:none;color:inherit} h1,h2,h3,h4{font-family:Georgia,\"Times New Roman\",serif;margin:0} p{margin:0} button,input,select,textarea{font:inherit}\n    .container{width:min(1180px,calc(100% - 56px));margin:0 auto}.hero-shell{position:relative;overflow:hidden;color:#fff;background:linear-gradient(110deg,#020816 0%, #08142f 38%, #263145 72%, #d7d5d1 125%),radial-gradient(circle at 18% 28%, rgba(8,76,198,.18), transparent 36%)}\n    .hero-shell::before{content:\"\";position:absolute;inset:0;background-image:linear-gradient(var(--grid) 1px, transparent 1px),linear-gradient(90deg,var(--grid) 1px, transparent 1px);background-size:92px 92px;pointer-events:none}\n    .nav{position:sticky;top:0;z-index:50;width:100%;padding:22px 0;backdrop-filter:blur(14px);background:linear-gradient(180deg, rgba(2,8,22,.72), rgba(2,8,22,.32));border-bottom:1px solid rgba(255,255,255,.06)} .nav-inner{display:flex;align-items:center;justify-content:space-between;gap:16px}.brand{display:flex;align-items:center;gap:12px;font-weight:800;letter-spacing:.02em}.brand-mark{width:42px;height:42px;border-radius:12px;display:grid;place-items:center;background:linear-gradient(135deg, rgba(214,178,93,0.22), rgba(255,255,255,0.08));border:1px solid rgba(214,178,93,0.24);overflow:hidden}.brand-mark img{width:100%;height:100%;object-fit:contain}.nav-links{display:flex;align-items:center;gap:14px;flex-wrap:wrap}.nav-link{padding:10px 14px;border-radius:999px;color:rgba(233,238,247,.76)}.nav-link:hover,.nav-link.active{color:#fff;background:rgba(255,255,255,.06)}.nav-cta{padding:11px 18px;border-radius:999px;color:#111;font-weight:700;background:linear-gradient(135deg,#d6b25d,#f3dfaa);box-shadow:0 10px 30px rgba(214,178,93,.22)}\n    .hero{position:relative;z-index:1;padding:46px 0 92px}.hero-grid{display:grid;grid-template-columns:1.02fr .98fr;gap:24px;align-items:end}.eyebrow{display:inline-flex;align-items:center;gap:10px;padding:10px 16px;border:1px solid rgba(255,255,255,.14);border-radius:999px;background:rgba(255,255,255,.06);color:#f3dfaa;font-size:.86rem;font-weight:700;letter-spacing:.04em;text-transform:uppercase;backdrop-filter:blur(10px);margin-bottom:18px}.hero h1{font-size:clamp(2.8rem,5vw,4.6rem);line-height:.96;letter-spacing:-.03em;max-width:11ch;margin-bottom:16px}.hero p{max-width:680px;color:rgba(255,255,255,.82);font-size:1rem;line-height:1.9}.hero-stats{display:grid;grid-template-columns:repeat(3,1fr);gap:12px}.hero-stat{padding:16px;border-radius:18px;background:rgba(255,255,255,.08);border:1px solid rgba(255,255,255,.08)}.hero-stat strong{display:block;font-size:1.5rem;margin-bottom:6px}.hero-stat span{color:rgba(255,255,255,.74);font-size:.86rem;line-height:1.5}\n    main{padding:0 0 80px}.panel-grid{display:grid;grid-template-columns:.76fr 1.24fr;gap:24px;margin-top:-48px;position:relative;z-index:4}.panel,.list-card,.empty-card{background:#fff;border-radius:28px;border:1px solid rgba(17,19,26,.06);box-shadow:var(--soft-shadow)}.panel{padding:22px}.profile-head{display:flex;gap:14px;align-items:flex-start}.avatar{width:62px;height:62px;border-radius:50%;display:grid;place-items:center;background:linear-gradient(135deg,#d6b25d,#f3dfaa);color:#111;font-weight:800;font-size:1.2rem;flex:none}.role-badge{display:inline-flex;align-items:center;padding:7px 10px;border-radius:999px;margin-top:10px;font-size:.74rem;font-weight:800;text-transform:uppercase;letter-spacing:.06em}.role-badge.buyer{background:rgba(71,209,140,.14);color:#0a8d4f}.role-badge.seller{background:rgba(255,107,107,.14);color:#db4d4d}.role-badge.investor{background:rgba(214,178,93,.14);color:#8a6700}.quick-links{display:grid;gap:10px;margin-top:18px}.quick-links a,.quick-links button{display:flex;justify-content:space-between;align-items:center;padding:14px 16px;border-radius:16px;background:var(--soft);border:1px solid rgba(17,19,26,.05);cursor:pointer}.quick-links a:hover,.quick-links button:hover{transform:translateY(-1px)}\n    .content-card{background:#fff;border-radius:28px;padding:22px;border:1px solid rgba(17,19,26,.06);box-shadow:var(--soft-shadow)}.tabs{display:flex;gap:10px;flex-wrap:wrap;margin-bottom:18px}.tab{padding:10px 16px;border-radius:999px;border:1px solid rgba(17,19,26,.08);background:#fff;cursor:pointer;font-weight:700}.tab.active{background:#0f1728;color:#fff;border-color:#0f1728}.section{display:none}.section.active{display:block}.section-head{display:flex;justify-content:space-between;align-items:flex-end;gap:16px;margin-bottom:16px}.section-head h2{font-size:2rem}.muted{color:var(--muted);line-height:1.7}.list{display:grid;gap:14px}.list-card{padding:18px;transition:transform .25s ease, box-shadow .25s ease}.list-card:hover{transform:translateY(-6px);box-shadow:0 20px 50px rgba(0,0,0,.18)}.item-top{display:flex;justify-content:space-between;gap:14px;align-items:flex-start}.item-top h3{font-size:1.3rem;margin-bottom:6px}.chips{display:flex;flex-wrap:wrap;gap:8px;margin:12px 0}.chip{padding:8px 12px;border-radius:999px;font-size:.8rem;font-weight:700}.chip.gold{background:rgba(242,201,76,.18);color:#7a5600;border:1px solid rgba(221,178,42,.18);box-shadow:0 0 12px rgba(242,201,76,.4)}.chip.soft{background:#f5f7fb;color:#334155;border:1px solid rgba(51,65,85,.08)}.item-actions{display:flex;flex-wrap:wrap;gap:10px;margin-top:14px}.btn{min-height:42px;padding:0 14px;border:none;border-radius:14px;font-weight:800;cursor:pointer;transition:all .2s ease}.btn:hover{transform:translateY(-2px);box-shadow:0 10px 24px rgba(0,0,0,.18)}.btn:active{transform:scale(.97)}.btn.primary{background:#0f1728;color:#fff}.btn.secondary{background:#eef3fb;color:#243244;border:1px solid rgba(36,50,68,.12)}.btn.gold{background:#fff7db;color:#6a4b00;border:1px solid rgba(221,178,42,.28)}.price{font-size:1.5rem;font-weight:800;color:#0f1728}.compare-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:14px}.compare-card{padding:18px;border-radius:22px;background:var(--soft);border:1px solid rgba(17,19,26,.06)}.notice{padding:16px 18px;border-radius:18px;background:#fff7db;color:#6a4b00;border:1px solid rgba(221,178,42,.28);margin-bottom:16px}.empty-card{padding:28px;text-align:center;color:var(--muted)}\n    @media (max-width: 980px){.hero-grid,.panel-grid,.compare-grid{grid-template-columns:1fr}.hero-stats{grid-template-columns:1fr}.nav-inner{flex-wrap:wrap}.nav-links{width:100%;justify-content:center}}\n  ";
const pageHtml = "\n<header class=\"hero-shell\">\n<div class=\"nav\"><div class=\"container nav-inner\"><a class=\"brand\" href=\"/\"><span class=\"brand-mark\"><img alt=\"SmartEstate logo\" src=\"/favicon.ico\"/></span><span>SmartEstate</span></a><div class=\"nav-links\"><a class=\"nav-link\" href=\"/\">Home</a><a class=\"nav-link\" href=\"/marketplace\">Marketplace</a><a class=\"nav-link\" href=\"/property-estimator\">Property AI</a><a class=\"nav-link\" href=\"/areas\">Areas</a><a class=\"nav-link\" href=\"/loans\">Loans</a><a class=\"nav-link\" href=\"/land\">Land</a><a class=\"nav-link active\" href=\"/dashboard\">Dashboard</a><a class=\"nav-cta\" href=\"/post-property\">Post Property</a></div></div></div>\n<div class=\"container hero\"><div class=\"hero-grid\"><div><span class=\"eyebrow\">SmartEstate account center</span><h1>Your role-based dashboard</h1><p>Manage saved listings, seller posts, compare board, and account shortcuts in one connected SmartEstate workspace.</p></div><div class=\"hero-stats\"><div class=\"hero-stat\"><strong id=\"statSaved\">0</strong><span>Saved listings</span></div><div class=\"hero-stat\"><strong id=\"statListings\">0</strong><span>My listings</span></div><div class=\"hero-stat\"><strong id=\"statCompare\">0</strong><span>Compare picks</span></div></div></div></div>\n</header>\n<main>\n<div class=\"container panel-grid\">\n<aside class=\"panel\">\n<div class=\"profile-head\"><div class=\"avatar\" id=\"avatar\">SE</div><div><h3 id=\"fullName\">SmartEstate User</h3><p class=\"muted\" id=\"userEmail\">Sign in to manage your space.</p><div class=\"role-badge buyer\" id=\"roleBadge\">Buyer</div></div></div>\n<div class=\"quick-links\">\n<a href=\"/marketplace\">Browse marketplace <span>\u2192</span></a>\n<a href=\"/post-property\">Post a property <span>\u2192</span></a>\n<button id=\"logoutBtn\" type=\"button\">Logout <span>\u2192</span></button>\n</div>\n</aside>\n<section class=\"content-card\">\n<div class=\"tabs\">\n<button class=\"tab active\" data-tab=\"saved\">Saved</button>\n<button class=\"tab\" data-tab=\"listings\">My Listings</button>\n<button class=\"tab\" data-tab=\"compare\">Compare</button>\n</div>\n<div class=\"section active\" id=\"savedSection\">\n<div class=\"section-head\"><div><h2>Saved listings</h2><p class=\"muted\">Your favorited SmartEstate properties across buyer and investor flows.</p></div></div>\n<div class=\"list\" id=\"savedList\"></div>\n</div>\n<div class=\"section\" id=\"listingsSection\">\n<div class=\"section-head\"><div><h2>My listings</h2><p class=\"muted\">Manage seller-posted properties, mark them sold, or remove them from your marketplace feed.</p></div><a class=\"btn gold\" href=\"/post-property\" style=\"display:inline-flex;align-items:center;justify-content:center;\">Create new listing</a></div>\n<div class=\"notice\" id=\"myListingsNotice\" style=\"display:none;\">Seller tools work best with a Seller account role, but you can still review any listings you posted locally.</div>\n<div class=\"list\" id=\"myListings\"></div>\n</div>\n<div class=\"section\" id=\"compareSection\">\n<div class=\"section-head\"><div><h2>Compare board</h2><p class=\"muted\">A simple side-by-side board for your shortlisted SmartEstate picks.</p></div><a class=\"btn gold\" href=\"/compare\" style=\"display:inline-flex;align-items:center;justify-content:center;\">View full comparison</a></div>\n<div class=\"compare-grid\" id=\"compareBoard\"></div>\n</div>\n</section>\n</div>\n</main>\n\n";
const pageScript = "\n    const API_BASE = 'http://127.0.0.1:5000';\n    const USER_KEY = 'smartestateUser';\n    const DASHBOARD_CACHE_PREFIX = 'smartestateDashboardCacheV2:';\n    const MARKETPLACE_CACHE_KEY = 'smartestateMarketplaceCacheV2';\n    const COMPARE_SNAPSHOT_KEY = 'smartestateCompareSnapshot';\n    const LEGACY_COMPARE_KEY = 'smartestateCompare';\n    const LEGACY_LISTINGS_KEY = 'smartestateListings';\n    const LEGACY_SELECTED_KEY = 'smartestateSelectedProperty';\n    const currentUser = JSON.parse(localStorage.getItem(USER_KEY) || 'null');\n    const tabs = document.querySelectorAll('.tab');\n    const dashboardState = { favorites: [], myListings: [], compare: [] };\n\n    function safeJsonParse(raw, fallback){ try { return JSON.parse(raw); } catch (error) { return fallback; } }\n    function getInitials(name){ return String(name || 'SE').split(/\\s+/).filter(Boolean).slice(0,2).map(part => part[0]).join('').toUpperCase(); }\n    function formatPrice(value){ return `JOD ${new Intl.NumberFormat('en-US').format(Number(value || 0))}`; }\n    function setCounts(saved, listings, compare){ document.getElementById('statSaved').textContent = saved; document.getElementById('statListings').textContent = listings; document.getElementById('statCompare').textContent = compare; }\n\n    function normalizeListing(item){\n      if(!item) return null;\n      const trust = item.trust || (String(item.trustLabel || '').toLowerCase().includes('ai') ? 'ai' : 'manual');\n      return { ...item, id:String(item.id), title:item.title || 'SmartEstate Listing', location:item.location || 'Amman', areaName:item.areaName || item.type || 'Property area', price:Number(item.price || 0), bedrooms:Number(item.bedrooms || 0), bathrooms:Number(item.bathrooms || 0), size:Number(item.size || 0), estimatedYield:item.estimatedYield == null ? null : Number(item.estimatedYield), demandScore:item.demandScore == null ? null : Number(item.demandScore), aiConfidence:item.aiConfidence == null ? null : Number(item.aiConfidence), trust, trustLabel:item.trustLabel || (trust === 'ai' ? 'AI Price Assisted' : 'Seller Price Only'), status:String(item.status || 'available').toLowerCase(), description:item.description || 'No description added yet.' };\n    }\n\n    function getLegacyListingPool(){\n      const selected = safeJsonParse(localStorage.getItem(LEGACY_SELECTED_KEY) || 'null', null);\n      const stored = safeJsonParse(localStorage.getItem(LEGACY_LISTINGS_KEY) || '[]', []);\n      const marketplaceCache = safeJsonParse(sessionStorage.getItem(MARKETPLACE_CACHE_KEY) || 'null', null);\n      const cachedListings = Array.isArray(marketplaceCache?.listings) ? marketplaceCache.listings : [];\n      const pool = [...stored, ...cachedListings];\n      if(selected) pool.unshift(selected);\n      const map = new Map();\n      for(const item of pool){ if(!item || item.id == null) continue; map.set(String(item.id), item); }\n      return Array.from(map.values());\n    }\n\n    function hydrateCompareFromIds(ids){\n      const pool = getLegacyListingPool();\n      return ids.map(id => pool.find(item => String(item.id) === String(id))).filter(Boolean).map(normalizeListing).filter(Boolean);\n    }\n\n    tabs.forEach(button => button.addEventListener('click', () => { tabs.forEach(tab => tab.classList.remove('active')); button.classList.add('active'); document.querySelectorAll('.section').forEach(section => section.classList.remove('active')); document.getElementById(`${button.dataset.tab}Section`).classList.add('active'); }));\n\n    function renderProfile(){ const name = currentUser?.fullName || currentUser?.name || 'SmartEstate User'; const role = String(currentUser?.role || 'buyer').toLowerCase(); document.getElementById('avatar').textContent = getInitials(name); document.getElementById('fullName').textContent = name; document.getElementById('userEmail').textContent = currentUser?.email || 'Log in to load your dashboard.'; const badge = document.getElementById('roleBadge'); badge.textContent = role.charAt(0).toUpperCase() + role.slice(1); badge.className = `role-badge ${['buyer','seller','investor'].includes(role) ? role : 'buyer'}`; }\n    function dashboardCard(item, actions=''){ return `<div class=\"list-card\"><div class=\"item-top\"><div><h3>${item.title}</h3><p class=\"muted\">${item.location || 'Amman'} \u2022 ${item.areaName || item.type || 'Property'}</p></div><div class=\"price\">${formatPrice(item.price)}</div></div><div class=\"chips\"><span class=\"chip gold\">${item.trustLabel || 'SmartEstate listing'}</span><span class=\"chip soft\">${item.bedrooms || 0} bed</span><span class=\"chip soft\">${item.bathrooms || 0} bath</span><span class=\"chip soft\">${item.size || 0} m\u00b2</span>${item.status ? `<span class=\"chip soft\">${String(item.status).toLowerCase() === 'sold' ? 'Sold' : 'Available'}</span>` : ''}</div><p class=\"muted\">${item.description || 'No description added yet.'}</p><div class=\"item-actions\"><button class=\"btn primary\" data-open=\"${item.id}\">Open details</button>${actions}</div></div>`; }\n    function renderSaved(){ const container = document.getElementById('savedList'); container.innerHTML = dashboardState.favorites.length ? dashboardState.favorites.map(item => dashboardCard(item, `<button class=\"btn secondary\" data-remove-favorite=\"${item.id}\">Remove favorite</button><button class=\"btn gold\" data-add-compare=\"${item.id}\">Compare</button>`)).join('') : '<div class=\"empty-card\">No saved listings yet.</div>'; }\n    function renderMyListings(){ const container = document.getElementById('myListings'); const notice = document.getElementById('myListingsNotice'); notice.style.display = currentUser ? 'none' : 'block'; notice.textContent = currentUser ? '' : 'Log in to manage your listings.'; container.innerHTML = dashboardState.myListings.length ? dashboardState.myListings.map(item => dashboardCard(item, `<a class=\"btn secondary\" href=\"post-property.html?edit=${item.id}\">Edit</a><button class=\"btn secondary\" data-toggle-status=\"${item.id}\">${String(item.status || 'available').toLowerCase() === 'sold' ? 'Mark available' : 'Mark sold'}</button><button class=\"btn gold\" data-delete=\"${item.id}\">Delete</button>`)).join('') : '<div class=\"empty-card\">You have not posted any listings yet.</div>'; }\n    function renderCompare(){ const container = document.getElementById('compareBoard'); container.innerHTML = dashboardState.compare.length ? dashboardState.compare.map(item => `<div class=\"compare-card\"><h3 style=\"font-size:1.25rem; margin-bottom:8px;\">${item.title}</h3><p class=\"muted\" style=\"margin-bottom:12px;\">${item.location || 'Amman'}</p><div class=\"chips\"><span class=\"chip gold\">${item.trustLabel || 'Listing'}</span>${item.estimatedYield != null ? `<span class=\"chip soft\">Yield ${item.estimatedYield}%</span>` : ''}${item.demandScore != null ? `<span class=\"chip soft\">Demand ${item.demandScore}</span>` : ''}</div><p class=\"muted\">${formatPrice(item.price)} \u2022 ${item.bedrooms || 0} bed \u2022 ${item.bathrooms || 0} bath \u2022 ${item.size || 0} m\u00b2</p><div class=\"item-actions\"><button class=\"btn primary\" data-open=\"${item.id}\">Open details</button><button class=\"btn secondary\" data-remove-compare=\"${item.id}\">Remove</button></div></div>`).join('') : '<div class=\"empty-card\" style=\"grid-column:1 / -1;\">No compare picks yet.</div>'; }\n    function renderAll(){ setCounts(dashboardState.favorites.length, dashboardState.myListings.length, dashboardState.compare.length); renderSaved(); renderMyListings(); renderCompare(); }\n    function cacheDashboard(data){ if(currentUser?.id) sessionStorage.setItem(`${DASHBOARD_CACHE_PREFIX}${currentUser.id}`, JSON.stringify(data)); }\n    function applyLocalCompare(items){ const normalized = (items || []).map(normalizeListing).filter(Boolean); dashboardState.compare = normalized; localStorage.setItem(COMPARE_SNAPSHOT_KEY, JSON.stringify(normalized)); localStorage.setItem(LEGACY_COMPARE_KEY, JSON.stringify(normalized.map(item => String(item.id)))); }\n    function getLocalCompare(){ const snapshot = safeJsonParse(localStorage.getItem(COMPARE_SNAPSHOT_KEY) || '[]', []); if(Array.isArray(snapshot) && snapshot.length) return snapshot; const legacyIds = safeJsonParse(localStorage.getItem(LEGACY_COMPARE_KEY) || '[]', []); if(Array.isArray(legacyIds) && legacyIds.length) return hydrateCompareFromIds(legacyIds); return []; }\n    function applyDashboard(data){ dashboardState.favorites = (data.favorites || []).map(normalizeListing).filter(Boolean); dashboardState.myListings = (data.myListings || []).map(normalizeListing).filter(Boolean); const backendCompare = (data.compare || []).map(normalizeListing).filter(Boolean); if(backendCompare.length) applyLocalCompare(backendCompare); else if(!dashboardState.compare.length) applyLocalCompare(getLocalCompare()); cacheDashboard(data); renderAll(); }\n    async function loadDashboard(){ const localCompare = getLocalCompare(); if(localCompare.length){ applyLocalCompare(localCompare); renderAll(); } if(!currentUser?.id){ renderAll(); return; } const cacheKey = `${DASHBOARD_CACHE_PREFIX}${currentUser.id}`; const cached = sessionStorage.getItem(cacheKey); if(cached){ try { applyDashboard(JSON.parse(cached)); } catch (error) {} } try { const response = await fetch(`${API_BASE}/api/users/${currentUser.id}/dashboard`); const data = await response.json(); if(!response.ok || !data.ok) throw new Error(data.message || 'Dashboard failed'); applyDashboard(data); } catch (error) { console.warn(error); renderAll(); } }\n    function openDetails(id){ location.href = `/property-details/${encodeURIComponent(id)}`; }\n    async function refreshDashboard(){ sessionStorage.removeItem(MARKETPLACE_CACHE_KEY); if(currentUser?.id) sessionStorage.removeItem(`${DASHBOARD_CACHE_PREFIX}${currentUser.id}`); await loadDashboard(); }\n    async function apiAction(url, options){ const response = await fetch(url, options); const data = await response.json(); if(!response.ok || !data.ok) throw new Error(data.message || 'Request failed'); return data; }\n    document.addEventListener('click', async event => { const openId = event.target.getAttribute('data-open'); const removeFavoriteId = event.target.getAttribute('data-remove-favorite'); const addCompareId = event.target.getAttribute('data-add-compare'); const removeCompareId = event.target.getAttribute('data-remove-compare'); const deleteId = event.target.getAttribute('data-delete'); const toggleStatusId = event.target.getAttribute('data-toggle-status'); try { if(openId) openDetails(openId); if(removeFavoriteId && currentUser?.id){ await apiAction(`${API_BASE}/api/users/${currentUser.id}/favorites/${removeFavoriteId}`, { method: 'DELETE' }); await refreshDashboard(); } if(addCompareId && currentUser?.id){ await apiAction(`${API_BASE}/api/users/${currentUser.id}/compare/${addCompareId}`, { method: 'POST' }); await refreshDashboard(); } if(addCompareId && !currentUser?.id){ const pool = getLegacyListingPool(); const picked = pool.find(item => String(item.id) === String(addCompareId)); if(picked){ const next = [...dashboardState.compare.filter(item => String(item.id) !== String(addCompareId)), normalizeListing(picked)].slice(-3); applyLocalCompare(next); renderAll(); } } if(removeCompareId && currentUser?.id){ await apiAction(`${API_BASE}/api/users/${currentUser.id}/compare/${removeCompareId}`, { method: 'DELETE' }); await refreshDashboard(); } if(removeCompareId && !currentUser?.id){ applyLocalCompare(dashboardState.compare.filter(item => String(item.id) !== String(removeCompareId))); renderAll(); } if(deleteId && currentUser?.id){ if(!confirm('Delete this listing?')) return; await apiAction(`${API_BASE}/api/listings/${deleteId}?ownerId=${encodeURIComponent(currentUser.id)}`, { method: 'DELETE' }); await refreshDashboard(); } if(toggleStatusId && currentUser?.id){ const item = dashboardState.myListings.find(listing => String(listing.id) === String(toggleStatusId)); const nextStatus = String(item?.status || 'available').toLowerCase() === 'sold' ? 'available' : 'sold'; await apiAction(`${API_BASE}/api/listings/${toggleStatusId}`, { method:'PUT', headers:{ 'Content-Type':'application/json' }, body:JSON.stringify({ ownerId: currentUser.id, status: nextStatus }) }); await refreshDashboard(); } } catch (error) { alert(error.message || 'Action failed.'); } });\n    document.getElementById('logoutBtn').addEventListener('click', () => { localStorage.removeItem(USER_KEY); if(currentUser?.id) sessionStorage.removeItem(`${DASHBOARD_CACHE_PREFIX}${currentUser.id}`); location.href = '/'; });\n    renderProfile(); renderAll(); loadDashboard();\n  ";

export default function DashboardPage() {
  useEffect(() => {
    if (!pageScript.trim()) return;
    const script = document.createElement("script");
    script.type = "text/javascript";
    script.text = pageScript;
    document.body.appendChild(script);
    document.dispatchEvent(new Event("DOMContentLoaded", { bubbles: true, cancelable: true }));
    return () => { script.remove(); };
  }, []);

  return (
    <>
      <style dangerouslySetInnerHTML={{ __html: pageCss }} />
      <div dangerouslySetInnerHTML={{ __html: pageHtml }} />
    </>
  );
}
